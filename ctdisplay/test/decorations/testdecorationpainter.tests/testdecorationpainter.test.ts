import * as assert from 'assert';

import {ThemableDecorationRenderOptions} from 'vscode';
import {Decorations} from '../../../src/decorations/decorations';
import * as fs from 'fs';
import * as vscode from 'vscode';

suite("Test decoration painter test", () => {

    test("Test lines must be the same like paint test decoration lines", () => {
    });

    test("Paint test decoration must paint in inputed text editor", () => {
    });
});